// include only the lodash functions that we use
export {
  // collections
  chunc, concat, drop, difference, fill, findIndex, intersection, join, map, remove, reverse, take, tail, union, uniq, without, each, find, filter,
  forEach, groupBy, includes, orderBy, reduce, some, sortBy,
  // functions
  debounce, delay, once, wrap,
  // lang
  isEmpty, cloneDeep, isArray,
  // object
  assign, forOwn, get, has, keys, merge, omit, set, values,
  // strings
  camelCase, lowerCase, capitalize, escape, kebabCase, replace, trim,
  // util
  attempt, range, uniqueId,
} from 'lodash';